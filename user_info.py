class userInfo:
    def __init__(self, user_id, username, full_name, total_follow):
        self.user_id = user_id
        self.username = username
        self.full_name = full_name
        self.total_follow = total_follow
